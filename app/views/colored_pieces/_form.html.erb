<%= form_for @colored_piece, :html => { :class => 'form-horizontal' } do |f| %>

    <%= f.input :code %>
    <%= f.association :piece, label_method: :code %>
    <%= f.association :color, label_method: :name %>
    <%= f.button :submit %>

<% end %>
